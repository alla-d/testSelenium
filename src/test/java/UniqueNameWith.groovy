def date = new Date().format("MMdd")
def time = new Date().format("HHmmss")

name = "Name " + date + "_" + time

// testRunner.getTestCase().setPropertyValue("name", name); // Useful for SoapUI properties
